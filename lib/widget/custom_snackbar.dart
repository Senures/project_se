/*  import 'package:flutter/material.dart';
import 'package:top_snackbar_flutter/custom_snack_bar.dart';
import 'package:top_snackbar_flutter/top_snack_bar.dart';

customSnackbar({required BuildContext context, required String type, required String message}) {
  switch (type) {
    case 'success':
      showTopSnackBar(
        context,
        CustomSnackBar.success(
          message: message,
        ),
      );
      break;
    case 'info':
      showTopSnackBar(
        context,
        CustomSnackBar.info(
          message: message,
        ),
      );
      break;
    case 'error':
      showTopSnackBar(
        context,
        CustomSnackBar.error(
          message: message,
        ),
      );
      break;
    default:
  }
}  */